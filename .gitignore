# Upstream clones + build outputs produced by bootstrap.sh (never committed)
work/
kit/
*.log
build/
.gradle/
__pycache__/
*.pyc
*.zip
.DS_Store

# session-local build-grind helper (sandbox memory workaround)
swap_watchdog.sh
